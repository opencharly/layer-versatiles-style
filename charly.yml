version: 2026.232.0520
versatiles-style:
    candy:
        version: 2026.144.1443
        description: |
            Local @versatiles/style MapLibre style-generator browser bundle for the Shortbread schema
            @versatiles/style is a TypeScript MapLibre style generator producing
            colorful() / eclipse() / graybeard() / neutrino() / shadow() /
            satellite() style JSON for maps backed by the Shortbread vector-tile
            schema. This candy installs nodejs + npm and lands the browser-ready
            bundle under /opt/versatiles-style/ (GitHub release tarball first, npm
            install fallback), so the notebook's MapLibre HTML imports the styles
            from a local file instead of a runtime CDN dependency (unpkg.com). The
            bundle is re-served as static JS by the versatiles-frontend layer at
            /style/versatiles-style.js. The install is verifiable: a non-empty
            versatiles-style*.js must land under the install path, and the node
            toolchain that built and serves it must be present.
        require:
            - '@github.com/opencharly/layer-supervisord:v2026.235.2056'
        distro:
            arch:
                package:
                    - nodejs
                    - npm
            fedora:
                package:
                    - nodejs
                    - npm
        plan:
            - run: mkdir=/opt/versatiles-style
              mkdir: /opt/versatiles-style
              run_as: root
            - run: |
                command=set -euo pipefail
                TMPDIR=$(mktemp -d)
                cd "$TMPDIR"

                # Tier 1: GitHub release tarball. The release ships THREE
                # tarballs — `versatiles-style.tar.gz`, `sprites.tar.gz`,
                # `styles.tar.gz`. We need the `versatiles-style.tar.gz` one
                # specifically (the JS bundle), so anchor the regex to the
                # exact filename rather than a prefix match (which would
                # alphabetically pick sprites.tar.gz first).
                if curl -fsSL "https://api.github.com/repos/versatiles-org/versatiles-style/releases/latest" \
                     | jq -r '.assets[].browser_download_url' \
                     | grep -E '/versatiles-style\.tar\.gz$' \
                     | head -1 > tarball_url \
                   && [ -s tarball_url ]; then
                  ASSET=$(cat tarball_url)
                  echo "Installing versatiles-style from GitHub release: $ASSET"
                  curl -fsSL "$ASSET" -o style.tar.gz
                  tar -xzf style.tar.gz -C /opt/versatiles-style --strip-components=0 || \
                    tar -xzf style.tar.gz -C /opt/versatiles-style
                else
                  # Tier 2: npm install
                  echo "Falling back to npm install @versatiles/style"
                  npm install --prefix "$TMPDIR" @versatiles/style --no-audit --no-fund
                  # Look for the prebuilt browser bundle the package ships in
                  # release/. If the package layout changed and release/
                  # doesn't exist, copy the whole package so versatiles-style.js
                  # is still findable via subsequent path-resolution.
                  PKG="$TMPDIR/node_modules/@versatiles/style"
                  if [ -d "$PKG/release" ]; then
                    cp -r "$PKG/release/." /opt/versatiles-style/
                  else
                    cp -r "$PKG/." /opt/versatiles-style/
                  fi
                fi

                # Sanity: the bundle must end up findable. The check probe
                # below asserts this, but fail loudly here too so the build
                # log surfaces the issue.
                if ! find /opt/versatiles-style -maxdepth 3 -name 'versatiles-style*.js' -size +1c | head -1 | grep -q .; then
                  echo "ERROR: no versatiles-style*.js found under /opt/versatiles-style after install" >&2
                  ls -la /opt/versatiles-style/
                  exit 1
                fi

                cd /
                rm -rf "$TMPDIR" /root/.npm
              run_as: root
              command: |
                set -euo pipefail
                TMPDIR=$(mktemp -d)
                cd "$TMPDIR"

                # Tier 1: GitHub release tarball. The release ships THREE
                # tarballs — `versatiles-style.tar.gz`, `sprites.tar.gz`,
                # `styles.tar.gz`. We need the `versatiles-style.tar.gz` one
                # specifically (the JS bundle), so anchor the regex to the
                # exact filename rather than a prefix match (which would
                # alphabetically pick sprites.tar.gz first).
                if curl -fsSL "https://api.github.com/repos/versatiles-org/versatiles-style/releases/latest" \
                     | jq -r '.assets[].browser_download_url' \
                     | grep -E '/versatiles-style\.tar\.gz$' \
                     | head -1 > tarball_url \
                   && [ -s tarball_url ]; then
                  ASSET=$(cat tarball_url)
                  echo "Installing versatiles-style from GitHub release: $ASSET"
                  curl -fsSL "$ASSET" -o style.tar.gz
                  tar -xzf style.tar.gz -C /opt/versatiles-style --strip-components=0 || \
                    tar -xzf style.tar.gz -C /opt/versatiles-style
                else
                  # Tier 2: npm install
                  echo "Falling back to npm install @versatiles/style"
                  npm install --prefix "$TMPDIR" @versatiles/style --no-audit --no-fund
                  # Look for the prebuilt browser bundle the package ships in
                  # release/. If the package layout changed and release/
                  # doesn't exist, copy the whole package so versatiles-style.js
                  # is still findable via subsequent path-resolution.
                  PKG="$TMPDIR/node_modules/@versatiles/style"
                  if [ -d "$PKG/release" ]; then
                    cp -r "$PKG/release/." /opt/versatiles-style/
                  else
                    cp -r "$PKG/." /opt/versatiles-style/
                  fi
                fi

                # Sanity: the bundle must end up findable. The check probe
                # below asserts this, but fail loudly here too so the build
                # log surfaces the issue.
                if ! find /opt/versatiles-style -maxdepth 3 -name 'versatiles-style*.js' -size +1c | head -1 | grep -q .; then
                  echo "ERROR: no versatiles-style*.js found under /opt/versatiles-style after install" >&2
                  ls -la /opt/versatiles-style/
                  exit 1
                fi

                cd /
                rm -rf "$TMPDIR" /root/.npm
            - check: a non-empty versatiles-style*.js browser bundle is present under /opt/versatiles-style
              exit_status: 0
              command: |
                find /opt/versatiles-style -maxdepth 3 -name 'versatiles-style*.js' -size +1c | head -1 | grep -q .
            - check: nodejs is installed so the @versatiles/style bundle can be built and served
              package:
                package: nodejs
                installed: true
            - check: the node runtime reports its version
              exit_status: 0
              stdout:
                - matches: "v[0-9]+"
              command: node --version
            - check: file=/opt/versatiles-style
              id: versatiles-style-install-dir
              file:
                file: /opt/versatiles-style
                filetype: directory

versatiles-style-skill:
    skill:
        name: versatiles-style
        family: versa
        owner: versatiles-style
        description: |-
            The @versatiles/style TypeScript package generates MapLibre style JSON for Shortbread vector tiles. Its local browser bundle avoids a CDN and is re-exported at /style/ for notebook iframe access.
            MUST be invoked before building, deploying, or troubleshooting the versatiles-style layer.
        content: |-
            # versatiles-style — MapLibre style generator for Shortbread
            
            ## Layer properties
            
            | Property | Value |
            |----------|-------|
            | Dependencies | `supervisord` (transitive; no service of its own) |
            | Distros | `arch` + `fedora` |
            | Build deps | nodejs, npm |
            | Install path | `/opt/versatiles-style/` |
            | Re-exported at | `/opt/versatiles-frontend/style/` (by the versatiles-frontend layer) |
            
            ## What it generates
            
            `versatiles-style` ships six style generators, all targeting the
            Shortbread vector-tile schema:
            
            | Function | Output character |
            |---|---|
            | `colorful({ language })` | Vivid colors, full feature set |
            | `eclipse({ language })` | Dark theme |
            | `graybeard({ language })` | Grayscale |
            | `neutrino({ language })` | Minimal / blueprint style |
            | `shadow({ language })` | Subtle hillshade-friendly |
            | `satellite({ language })` | Satellite-imagery overlay style |
            
            Each returns a MapLibre style JSON object (`style.version`, `.sources`,
            `.layers`, `.glyphs`, `.sprite`). The default source URL points at
            `tiles.versatiles.org`; the notebook patches `style.sources.versatiles.tiles`
            to our local versatiles serve.
            
            ## Why install locally
            
            The package's README documents both an npm-install path and a
            standalone GitHub release tarball. We prefer the GitHub release
            tarball (`versatiles-style.tar.gz`) because it ships the browser-
            ready bundle directly. The layer has a two-tier install:
            
            1. **Tier 1**: download `versatiles-style.tar.gz` from the latest
               GitHub release and extract to `/opt/versatiles-style/`.
            2. **Tier 2** (fallback): `npm install @versatiles/style` and copy
               `node_modules/@versatiles/style/release/` (or the whole package
               if `release/` is missing) to `/opt/versatiles-style/`.
            
            The fallback covers cases where the GitHub release is missing the
            expected tarball asset.
            
            ## Iframe-access pattern
            
            The notebook's shortbread MapLibre cell uses `mo.iframe`, whose
            `srcdoc` HTML can only fetch absolute URLs (not server-relative
            paths against the marimo edit server). The bundle is re-exported
            at `/opt/versatiles-frontend/style/versatiles-style.js` by the
            versatiles-frontend layer's `python -m http.server`, so the
            iframe references:
            
            ```html
            <script src="http://127.0.0.1:28002/style/versatiles-style.js"></script>
            ```
            
            Single-origin against the versatiles-frontend service avoids
            cross-origin CORS issues that would otherwise need explicit
            `Access-Control-Allow-Origin: *` headers.
            
            ## Check probes
            
            Build-scope:
            - `versatiles-style-bundle-present` — `find /opt/versatiles-style -maxdepth 3 -name 'versatiles-style*.js' -size +1c` matches at least one non-empty file (the install path's exact filename depends on package version; the find pattern is lenient)
            
            ## Cross-references
            
            - `/charly-versa:versa` — image composing this layer
            - `/charly-versa:versatiles` — the tile-server backend the generated
              styles point at after URL patching
            - `/charly-versa:versatiles-frontend` — re-exports this bundle at
              `/style/` for iframe consumption
            - `/charly-versa:shortbread` — the tile schema the styles render
            - `/charly-versa:notebook-osm` — the shortbread MapLibre cell that
              imports `colorful()` from the bundle
